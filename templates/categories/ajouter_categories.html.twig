{% extends "base.html.twig" %}

{% block title %}
CATEGORIES
{% endblock %}

{% block body %}
<div class="container shadow rounded mx-auto w-50 mt-3 p-3">
    {{form_start(form)}}
    {{form(form)}}
    {{form_end(form)}}
    <a href="{{path('app_categories')}}" class="btn btn-info">Retour</a>
</div>
{% endblock %}